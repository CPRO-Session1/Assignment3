/*Ava N.*/
/*This has half a diamond on request of the user's preference of length*/
#include <stdio.h>
#include <stdlib.h>

int main() {
int length; /*the user's input for the length of the half diamond*/
printf("Write a positive integer for the length you want for half a diamond \n");
scanf("%d", &length);
int line;
	for (line=1; line<=1; line++);
		printf("* \n");
	{
		print("\n")
		int row;
		for (row=1; row<line; row--);
		row=line-1;
		printf("* \n");
	}

	return 0;

}
